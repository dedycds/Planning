<h1>Project_Type creation</h1>

<form action="{{ path('project_type_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('project_type') }}">
            Back to the list
        </a>
    </li>
</ul>
